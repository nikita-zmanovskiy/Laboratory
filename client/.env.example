# Оставьте пустым — запросы идут через Next.js proxy (/api → backend), cookies на том же origin
NEXT_PUBLIC_API_URL=

# URL бэкенда для rewrites в next.config.ts (только серверная сторона)
BACKEND_URL=http://localhost:3000
