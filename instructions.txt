
Из корня проекта по очереди:
backend-nest
npm run build
npm run lint
npm run format
npm run check
npm run fix


Потом:
cd ../client
pnpm check
pnpm typecheck
pnpm build



Docker (из корня Laboratory): backend-nest + client + nginx + postgres + redis

1 Один раз: скопировать и заполнить секреты (GigaChat, CSRF)
 cp .env.production.example .env.production

2 Пересобрать образы (без кэша)
docker compose --env-file .env.production build --no-cache

3 Поднять стек заново
docker compose --env-file .env.production up -d --force-recreate
Проверка:
 docker compose --env-file .env.production ps
 curl http://localhost/health
 docker compose --env-file .env.production logs -f backend

Остановить:
  docker compose --env-file .env.production down
С удалением БД:
 docker compose --env-file .env.production down -v

для тестов - npm run test:all

k6
npm run load:health
npm run load:csrf
npm run load:generate
